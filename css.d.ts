// Ambient declarations for non-code side-effect imports (CSS).
// TypeScript 6+ flags side-effect imports of untyped modules (TS2882); the
// bundler (Next.js) handles these at build time.
declare module '*.css';
